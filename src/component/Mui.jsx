import React from "react";
import { Typography } from "@mui/material";

const Mui = () => {
  return (
    <div>
      <Typography variant="h1">mostafa</Typography>
      <Typography variant="h2">mostafa</Typography>
      <Typography variant="h3">mostafa</Typography>
      <Typography variant="h4">mostafa</Typography>
      <Typography variant="h5">mostafa</Typography>
      <Typography variant="h6" gutterBottom>
        mostafa
      </Typography>
      <Typography variant="subtitle1">mostafa</Typography>
      <Typography variant="subtitle2">mostafa</Typography>
      <Typography variant="body1">mostafa</Typography>
      <Typography variant="body2">mostafa</Typography>
    </div>
  );
};

export default Mui;
